{
  "name": "@messaging-app/api-types",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript types generated from the API's OpenAPI document. Regenerate with `pnpm generate:api-types` from the repository root.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "openapi-typescript ./openapi.json -o ./src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"no lint configured for generated types\""
  },
  "devDependencies": {
    "openapi-typescript": "^7.5.0",
    "typescript": "^5.7.3"
  }
}
