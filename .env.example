# Copy this file to `.env` in the repository root before running
# `docker compose`. Values below are local-development-only placeholders,
# not real credentials, and `.env` itself is gitignored.

POSTGRES_USER=messaging_app
POSTGRES_PASSWORD=local_dev_only
POSTGRES_DB=messaging_app
POSTGRES_PORT=5432
