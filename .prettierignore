node_modules
.next
dist
coverage
.turbo
pnpm-lock.yaml

# Pre-existing project-governance and process artifacts owned outside this
# ticket's scope. MA-2 adds the formatting command; it does not reformat
# unrelated, already-committed files.
.claude/**
.mcp.json
AGENTS.md
CLAUDE.md
docs/plans/**
docs/setup/**

# Generated shared API types; regenerated by openapi-typescript and not
# hand-formatted.
packages/api-types/src/index.ts

# Scratch and fixture output. scripts/test_agent_guards.ps1 writes here on
# every run, and review agents use it as a working copy, so leaving it
# unignored makes format:check fail for reasons unrelated to the repository.
tmp/
